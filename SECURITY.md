# 安全说明

## 支持范围

当前版本仍处于 `0.x` 阶段。额度接口、Kindle 固件内部命令和第三方登录格式都可能变化。

## 凭据边界

- 不要把令牌、Cookie、浏览器用户目录或登录凭据提交到仓库。
- DeepSeek 密钥只能通过环境变量提供。
- Claude 与 Kimi 采集器默认关闭，并且必须显式设置
  `allowLocalCredentialRead: true` 才会读取本机登录文件。
- 采集器不会把令牌写入输出数据或日志。
- Kimi 采集器不会修改或刷新登录凭据；令牌过期时只会返回失败状态。
- `state/`、`dist/`、`history/` 与日志默认被 Git 忽略。

## 公开部署警告

GitHub Pages 是公开网页。写入 `data.json` 的额度、余额、天气、地点和句子都会对知道网址的人可见。需要隐私时，使用局域网部署，并关闭天气、地点和每日一语。

## 报告问题

不要在公开 Issue 中粘贴令牌、完整日志或凭据文件。发现可能泄露秘密的问题时，先通过仓库的私密安全报告功能联系维护者。
