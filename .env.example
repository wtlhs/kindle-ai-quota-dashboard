# 这里只写环境变量名称的示例，不要把真实值提交到 Git。
GLM1_API_KEY=
GLM2_API_KEY=
KIMI_API_KEY=
MINIMAX_API_KEY=
# 每日一语生成（可选）：默认复用 GLM1_API_KEY，也可单独指定
QUOTE_API_KEY=
QUOTE_MODEL=
KINDLE_QUOTA_CONFIG=
# 推送到自建服务器（替代 GitHub Pages）：rsync 目标与 SSH 端口
DEPLOY_TARGET=
DEPLOY_SSH_PORT=22
DASHBOARD_URL=
PORT=8787
